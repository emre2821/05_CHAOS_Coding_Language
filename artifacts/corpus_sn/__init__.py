"""Example CHAOS scripts bundled with the distribution.

These files are shipped so users can reference them directly via the
``artifacts/corpus_sn/`` path when running the CLI after installation.
"""
